version: '3'
services: 
  worker0:
    image: "brianbbsu/cms:YTP2020"
    network_mode: "host"
    tty: true
    restart: unless-stopped
    volumes: 
      - "./cms.conf:/usr/local/etc/cms.conf:ro"
    privileged: true
    command: "cmsWorker 0"
